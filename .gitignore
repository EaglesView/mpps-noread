# derived / output artifacts
keystream.bin
ks.bin
out.bin
*.dec

# regenerable test vectors (run make_testvec.py)
test_plain.bin
test_noread.Bin
__pycache__/
*.pyc

# reference tunes / dumps are large binaries — keep them out of git by default
# (comment these out if you want to version a specific reference)
*.frf

# MPPS program (proprietary binaries, drivers, per-ECU check DLLs)
mpps/

# ECU dumps / tunes
tunes/
*.ori
*.Bin
*.bin
*.json
*.xml

# emulation venv / rootfs
.venv-emu/
emu-rootfs/
module_dump*.bin
tricore_dump*.bin
*_live_*.bin
*_unpacked_*.bin
mpps_module.bin
mpps_live_*.bin

# cloned external tool (tune-editor)
tuneviewer/

# proprietary / scraped (not part of the release)
tools/*.exe
tuneData_scraped/
